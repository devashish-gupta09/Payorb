import { makeStyles } from "@material-ui/core";

export const styles = makeStyles(() => ({
  signupButton: {
    background: "linear-gradient(115.52deg, #68FDF3 0%, #00D4FF 100%)",
    borderRadius: "2em",
    padding: "0.75em 1em",
    textTransform: "capitalize",
  },
}));
