import { makeStyles } from "@material-ui/core";

export const globalStyles = makeStyles({
    bold: {
        fontWeight: "bold"
    },
    boldSixHundred: {
        fontWeight: "600"
    },
    bold500: {
        fontWeight: "500"
    }
})
